# 📦 Скрипты развертывания

Эта папка содержит все скрипты для развертывания Telegram бота на Ubuntu сервере через Git.

## 📋 Описание скриптов

### `deploy.sh`
Базовый скрипт установки системы:
- Устанавливает Node.js 20.x
- Устанавливает PM2 для управления процессами
- Устанавливает Nginx
- Устанавливает Certbot (для Let's Encrypt)
- Устанавливает Git
- Настраивает базовую конфигурацию Nginx
- Настраивает файрвол

**Использование:**
```bash
sudo ./deploy/deploy.sh
```

### `setup-regru-ssl.sh`
Настройка SSL сертификата от Reg.ru:
- Помогает загрузить сертификаты из панели Reg.ru
- Настраивает Nginx с SSL
- Настраивает редирект HTTP → HTTPS

**Использование:**
```bash
sudo ./deploy/setup-regru-ssl.sh
```

### `deploy-full.sh` ⭐
Полный скрипт развертывания через Git (рекомендуется):
- Выполняет все шаги автоматически
- Клонирует репозиторий из GitHub
- Включает установку, настройку SSL, развертывание кода и запуск

**Использование:**
```bash
sudo ./deploy/deploy-full.sh
```

**Параметры:**
- По умолчанию использует: `https://github.com/Raleska/budbot.git`
- Ветка: `main`
- Можно изменить при запуске скрипта

### `deploy-update.sh` 🔄
Скрипт для обновления бота через Git:
- Создает резервную копию
- Обновляет код через `git pull`
- Устанавливает новые зависимости
- Перезапускает бота

**Использование:**
```bash
sudo ./deploy/deploy-update.sh
```

## 📄 Вспомогательные файлы

### `nginx.conf.example`
Пример конфигурации Nginx для бота. Используется как шаблон при настройке веб-сервера.

### `env.production.example`
Пример файла переменных окружения для продакшена. Скопируйте в `.env` и заполните значения.

## 🚀 Быстрый старт

### Первое развертывание

1. **Подключитесь к серверу:**
   ```bash
   ssh root@ваш_сервер
   ```

2. **Клонируйте репозиторий:**
   ```bash
   cd /opt
   git clone https://github.com/Raleska/budbot.git telegram-bot
   cd telegram-bot
   ```

3. **Запустите полный скрипт развертывания:**
   ```bash
   chmod +x deploy/*.sh
   sudo ./deploy/deploy-full.sh
   ```

### Обновление бота

После внесения изменений в репозиторий на GitHub:

```bash
cd /opt/telegram-bot
sudo ./deploy/deploy-update.sh
```

Или вручную:

```bash
cd /opt/telegram-bot
sudo -u botuser git pull
sudo -u botuser npm install  # если изменились зависимости
sudo -u botuser pm2 restart telegram-bot
```

## ⚙️ Настройка переменных

Перед запуском убедитесь, что вы настроили:

1. **Домен** в скриптах (по умолчанию `hyalpharmbot.ru`)
2. **Пользователь бота** (по умолчанию `botuser`)
3. **Директорию приложения** (по умолчанию `/opt/telegram-bot`)
4. **Git репозиторий** (по умолчанию `https://github.com/Raleska/budbot.git`)

Отредактируйте переменные в начале каждого скрипта при необходимости.

## 🔒 Безопасность

- Все скрипты требуют прав root
- SSL сертификаты должны иметь правильные права доступа (600 для ключа, 644 для сертификата)
- Используйте `WEBHOOK_SECRET_TOKEN` для защиты вебхука
- Не храните `.env` файл в системе контроля версий
- Для приватных репозиториев используйте SSH ключи или Personal Access Token

## 🔄 Работа с Git

### Публичный репозиторий
Просто используйте HTTPS URL:
```bash
git clone https://github.com/Raleska/budbot.git
```

### Приватный репозиторий

**Вариант 1: SSH ключ (рекомендуется)**
```bash
# Генерация SSH ключа на сервере
ssh-keygen -t ed25519 -C "bot@server"
cat ~/.ssh/id_ed25519.pub
# Добавьте публичный ключ в GitHub Settings > SSH and GPG keys

# Клонирование через SSH
git clone git@github.com:Raleska/budbot.git
```

**Вариант 2: Personal Access Token**
```bash
# Создайте токен в GitHub Settings > Developer settings > Personal access tokens
# Клонирование с токеном
git clone https://YOUR_TOKEN@github.com/Raleska/budbot.git
```

## 📚 Дополнительная документация

- `../DEPLOYMENT.md` - Подробная инструкция по развертыванию
- `../QUICK_DEPLOY.md` - Краткая инструкция
- `../README_WEBHOOK.md` - Настройка вебхука
- `../README_DATABASE.md` - Настройка базы данных
