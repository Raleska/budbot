# Редактирование текстов бота

Все тексты бота хранятся в файле `config/texts.json` для удобного редактирования без изменения кода.

## 📝 Как редактировать тексты

1. **Откройте файл `config/texts.json`** в любом текстовом редакторе
2. **Найдите нужный текст** по ключу (например, `WELCOME`, `SELECT_DOSAGE`)
3. **Измените текст** на нужный вам
4. **Сохраните файл**
5. **Перезапустите бота** для применения изменений

## 🔤 Форматирование текста

### Переносы строк
Используйте `\n` для переноса строки:
```json
"WELCOME": "Первая строка\n\nВторая строка"
```

### Жирный текст (акценты)
Используйте `**текст**` для жирного текста:
```json
"WELCOME": "Добро пожаловать в **Hyalpharm Flow**"
```

### Плейсхолдеры (переменные)
Используйте фигурные скобки для подстановки значений:
- `{time}` - время напоминания
- `{time1}` - первое время (для двух раз в день)
- `{time2}` - второе время (для двух раз в день)
- `{timezone}` - часовой пояс

Пример:
```json
"CONFIRM_TIME_SINGLE": "Вы будете получать уведомления в **{time}**"
```

**Примечание:** Markdown форматирование включено автоматически для всех сообщений.

## 📋 Список всех текстов

### Основные сообщения
- `WELCOME` - Приветственное сообщение при `/start`
- `SELECT_DOSAGE` - Выбор частоты приема (один/два раза в день)
- `SELECT_TIMEZONE` - Выбор часового пояса
- `SELECT_TIME_SINGLE` - Выбор времени (один раз в день)
- `SELECT_TIME_FIRST` - Выбор первого времени (два раза в день)
- `SELECT_TIME_SECOND` - Выбор второго времени (два раза в день)
- `ENTER_CUSTOM_TIME` - Ввод своего времени

### Подтверждения
- `CONFIRM_TIME_SINGLE` - Подтверждение времени (один раз в день)
  - Параметры: `{time}`
- `CONFIRM_TIME_FIRST` - Подтверждение первого времени (два раза в день)
  - Параметры: `{time}`
- `CONFIRM_TIME_SECOND` - Подтверждение второго времени (два раза в день)
  - Параметры: `{time}`

### Финальные сообщения
- `REMINDER_SET_SINGLE` - Сообщение после создания напоминания (один раз в день)
  - Параметры: `{time}`, `{timezone}`
- `REMINDER_SET_DOUBLE` - Сообщение после создания напоминания (два раза в день)
  - Параметры: `{time1}`, `{time2}`, `{timezone}`

### Напоминания и детали
- `REMINDER_MESSAGE` - Текст ежедневного напоминания
- `ACTIVE_REMINDERS_LIST` - Список активных напоминаний
- `REMINDER_DETAILS_ONE` - Детали напоминания (один раз в день)
  - Параметры: `{time}`
- `REMINDER_DETAILS_TWO` - Детали напоминания (два раза в день)
  - Параметры: `{time}`
- `NO_ACTIVE_REMINDERS` - Сообщение когда нет активных напоминаний

### Другое
- `ABOUT_COMPANY` - Текст раздела "О компании"
- `INVALID_TIME_FORMAT` - Ошибка неверного формата времени
- `INVALID_TIME_RANGE` - Ошибка неверного диапазона времени

## 🔘 Кнопки (BUTTONS)

Все тексты кнопок находятся в секции `BUTTONS`:
- `START_VITAMINS` - "Настроить прием"
- `ABOUT_COMPANY` - "О компании"
- `ACTIVE_REMINDERS` - "Активные напоминания"
- `ONE_CAPSULE` - "Один раз в день"
- `TWO_CAPSULES` - "Два раза в день"
- `BACK` - "Назад"
- `CONFIRM` - "Подтвердить"
- `CUSTOM_TIME` - "Другое время"
- `MAIN_MENU` - "Главное меню"
- `TIME_8`, `TIME_12`, `TIME_18`, `TIME_21` - Времена на кнопках
- `EDIT_CAPSULES` - "Изменить частоту"
- `EDIT_TIME` - "Изменить время"
- `DELETE_REMINDER` - "Отменить напоминание"

## 🌍 Часовые пояса (TIMEZONES)

Список часовых поясов находится в секции `TIMEZONES`. Каждый часовой пояс имеет:
- `label` - Отображаемое название (например, "UTC +3:00 Москва, Киев, Стамбул")
- `value` - Значение для системы (например, "UTC+3")

Для добавления нового часового пояса добавьте объект в массив:
```json
{
  "label": "UTC +13:00 Ваш город",
  "value": "UTC+13"
}
```

## ⚠️ Важные замечания

1. **Синтаксис JSON**: Обязательно используйте правильный синтаксис JSON:
   - Все строки должны быть в двойных кавычках `"`
   - После каждого элемента (кроме последнего) должна быть запятая `,`
   - Не используйте комментарии в JSON (кроме файла `texts.example.json`)

2. **Экранирование**: Если используете кавычки внутри текста, экранируйте их: `\"`
   ```json
   "NO_ACTIVE_REMINDERS": "Настройте напоминание, нажав \"📅 Настроить прием\""
   ```

3. **Перезапуск**: После изменения текстов обязательно перезапустите бота

4. **Резервная копия**: Рекомендуется сделать резервную копию `texts.json` перед редактированием

## 📄 Пример файла

Смотрите `config/texts.example.json` для примера структуры и комментариев.

## 🔄 Восстановление

Если файл `texts.json` поврежден или удален:
1. Скопируйте `config/texts.example.json` в `config/texts.json`
2. Отредактируйте тексты по необходимости
3. Перезапустите бота
