# ⚡ Быстрое развертывание на Ubuntu

Краткая инструкция для быстрого развертывания бота на Ubuntu сервере с доменом от Reg.ru.

## 📋 Что нужно перед началом

✅ Домен с настроенными A записями на ваш IP  
✅ SSL сертификат от Reg.ru (или готовность получить от Let's Encrypt)  
✅ Доступ по SSH к серверу с правами root  
✅ Telegram Bot Token от @BotFather  

## 🚀 Шаги развертывания

### 1. Копирование файлов на сервер

```bash
# На вашем локальном компьютере
scp -r . root@ваш_сервер_ip:/opt/telegram-bot/
```

### 2. Подключение к серверу

```bash
ssh root@ваш_сервер_ip
cd /opt/telegram-bot
```

### 3. Запуск автоматического развертывания

```bash
chmod +x deploy/*.sh
sudo ./deploy/deploy-full.sh
```

Скрипт выполнит:
- ✅ Установку Node.js, PM2, Nginx
- ✅ Настройку SSL сертификата от Reg.ru
- ✅ Установку зависимостей
- ✅ Настройку базы данных (если нужно)
- ✅ Запуск бота
- ✅ Установку вебхука

### 4. Настройка переменных окружения

Во время выполнения скрипта откроется файл `.env` для редактирования. Заполните:

```env
BOT_TOKEN=ваш_токен_от_BotFather
USE_DATABASE=true
USE_WEBHOOK=true
WEBHOOK_URL=https://hyalpharmbot.ru
WEBHOOK_SECRET_TOKEN=сгенерируйте_через_openssl_rand_-hex_32
DB_PASSWORD=ваш_пароль_postgresql
```

### 5. Проверка работы

```bash
# Проверка статуса бота
pm2 status

# Просмотр логов
pm2 logs telegram-bot

# Проверка вебхука
npm run webhook:info

# Проверка health check
curl https://hyalpharmbot.ru/health
```

## 🔒 Настройка SSL от Reg.ru

Если скрипт не настроил SSL автоматически:

1. **Скачайте файлы из панели Reg.ru:**
   - Сертификат (Certificate)
   - Private Key
   - Корневой сертификат (если есть)

2. **Загрузите на сервер:**
   ```bash
   scp certificate.crt root@сервер:/etc/nginx/ssl/hyalpharmbot.ru/
   scp private.key root@сервер:/etc/nginx/ssl/hyalpharmbot.ru/
   ```

3. **Запустите скрипт настройки SSL:**
   ```bash
   chmod +x deploy/setup-regru-ssl.sh
   sudo ./deploy/setup-regru-ssl.sh
   ```

## 📚 Подробная документация

См. `DEPLOYMENT.md` для детальных инструкций.
