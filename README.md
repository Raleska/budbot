# Telegram Bot - Напоминания о витаминах

Telegram бот для настройки напоминаний о приеме витаминов с понятной архитектурой для быстрого изменения текстов и user flow.

> 💡 **Редактирование текстов**: Все тексты бота находятся в `config/texts.json` - просто откройте файл и измените нужные тексты! См. `EDIT_TEXTS.md` для быстрого старта.

> 🚀 **Развертывание на сервере**: См. `DEPLOYMENT.md` для инструкций по развертыванию на Ubuntu сервере с доменом от Reg.ru. Все скрипты развертывания находятся в папке `deploy/`.

## 🏗️ Архитектура

Проект организован модульно для удобства поддержки и изменений:

```
bot_damir_remind/
├── config/              # Конфигурация и константы
│   ├── texts.js        # Все тексты бота (легко менять)
│   └── states.js       # Состояния user flow
├── handlers/           # Обработчики для каждого шага
│   ├── startHandler.js
│   ├── dosageHandler.js
│   ├── capsuleSelectionHandler.js
│   ├── timezoneHandler.js
│   ├── timeSelectionHandler.js
│   ├── customTimeHandler.js
│   ├── confirmationHandler.js
│   └── mainMenuHandler.js
├── services/           # Бизнес-логика
│   └── userState.js    # Управление состояниями пользователей
├── utils/              # Вспомогательные функции
│   ├── keyboards.js    # Клавиатуры для кнопок
│   └── validators.js   # Валидация данных
├── index.js            # Главный файл бота
├── package.json
└── README.md
```

## 🚀 Установка и запуск

### Предварительные требования

- **Node.js** версии 16 или выше
- **npm** (обычно устанавливается вместе с Node.js)
- **Telegram аккаунт** и токен бота от [@BotFather](https://t.me/BotFather)

### Шаг 1: Получение токена бота

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям: укажите имя и username для бота
4. После создания бота BotFather предоставит вам **токен** (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)
5. Сохраните этот токен — он понадобится на следующем шаге

### Шаг 2: Клонирование и установка

1. **Клонируйте репозиторий** (если используете git) или перейдите в папку проекта:
```bash
cd bot_damir_remind
```

2. **Установите зависимости:**
```bash
npm install
```

Это установит все необходимые пакеты:
- `telegraf` - библиотека для работы с Telegram Bot API
- `node-cron` - для планирования напоминаний
- `dotenv` - для работы с переменными окружения

### Шаг 3: Настройка переменных окружения

1. **Создайте файл `.env`** в корне проекта (на том же уровне, что и `package.json`)

2. **Добавьте в файл `.env`** ваш токен бота:
```
BOT_TOKEN=ваш_токен_от_BotFather
```

Например:
```
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```

⚠️ **Важно:** 
- Не коммитьте файл `.env` в git (он уже добавлен в `.gitignore`)
- Никогда не делитесь своим токеном публично

### Шаг 4: Запуск бота

**Режимы запуска:**

1. **С базой данных (PostgreSQL) - по умолчанию:**
   ```bash
   npm start
   ```
   > Требуется настроить PostgreSQL (см. `README_DATABASE.md`)

2. **Без базы данных (in-memory):**
   ```bash
   npm run start:memory
   ```
   > Данные хранятся только в памяти и теряются при перезапуске

3. **С вебхуком и БД:**
   ```bash
   npm run start:webhook
   ```
   > ⚠️ Для вебхука необходимо настроить переменные окружения в `.env`:
   > - `USE_WEBHOOK=true` (устанавливается автоматически)
   > - `WEBHOOK_URL=https://your-domain.com` (URL вашего сервера)
   > - `WEBHOOK_PORT=3000` (опционально, по умолчанию 3000)
   > - `WEBHOOK_PATH=/webhook` (опционально, по умолчанию /webhook)
   > - `WEBHOOK_SECRET_TOKEN=your_secret_token` (опционально, для безопасности)

4. **С вебхуком без БД:**
   ```bash
   npm run start:webhook:memory
   ```

**Для разработки с автоперезагрузкой** (бот перезапустится при изменении файлов):
```bash
npm run dev
```

Если всё настроено правильно, вы увидите сообщение:
```
✅ Бот успешно запущен!
```

### Шаг 5: Проверка работы

1. Откройте Telegram
2. Найдите вашего бота по username, который вы указали при создании
3. Отправьте команду `/start`
4. Бот должен ответить приветственным сообщением с кнопками

## 🔧 Как работает проект

### Принцип работы

Бот использует **систему состояний (state machine)** для управления диалогом с пользователем:

1. **Каждый пользователь имеет своё состояние** - текущий шаг в диалоге
2. **Состояния хранятся в памяти** (в продакшене можно заменить на базу данных)
3. **Роутинг сообщений** происходит на основе текущего состояния пользователя

### Поток данных

```
Пользователь → Telegram → index.js → Определение состояния → Обработчик → Ответ пользователю
```

### Основные компоненты

#### 1. `index.js` - Главный файл
- Инициализирует бота с токеном
- Настраивает обработчики команд (`/start`)
- Роутит текстовые сообщения по состояниям пользователя
- Обрабатывает ошибки

#### 2. `services/userState.js` - Управление состояниями
- Хранит текущее состояние каждого пользователя
- Сохраняет данные пользователя (количество капсул, время, часовой пояс)
- Предоставляет методы для работы с состояниями

#### 3. `handlers/` - Обработчики шагов
Каждый файл отвечает за один шаг диалога:
- `startHandler.js` - приветствие
- `dosageHandler.js` - переход к выбору дозировки
- `capsuleSelectionHandler.js` - выбор 1 или 2 капсул
- `timezoneHandler.js` - выбор часового пояса
- `timeSelectionHandler.js` - выбор времени из предложенных
- `customTimeHandler.js` - ввод своего времени
- `confirmationHandler.js` - подтверждение настроек
- `mainMenuHandler.js` - возврат в главное меню

#### 4. `config/texts.js` - Все тексты
- Все сообщения бота в одном месте
- Легко изменять без правки кода
- Поддержка шаблонов с параметрами

#### 5. `utils/keyboards.js` - Клавиатуры
- Создание кнопок для каждого шага
- Автоматическое изменение размера клавиатуры

#### 6. `utils/validators.js` - Валидация
- Проверка формата времени (ЧЧ:ММ)
- Нормализация ввода пользователя

### Пример работы системы состояний

```
1. Пользователь: /start
   → Состояние: START
   → Обработчик: startHandler
   → Ответ: Приветствие + кнопки

2. Пользователь: "📅 Начать прием витаминов"
   → Состояние: SELECT_DOSAGE
   → Обработчик: capsuleSelectionHandler
   → Ответ: "Сколько капсул?" + кнопки

3. Пользователь: "1 капсула ⭐ рекомендовано"
   → Состояние: SELECT_TIMEZONE
   → Обработчик: timezoneHandler
   → Сохранение: { capsules: 1 }
   → Ответ: "Выберите часовой пояс" + кнопки

... и так далее
```

### Хранение данных

**Текущая реализация:**
- Состояния и данные хранятся в памяти (объект `Map`)
- При перезапуске бота все данные теряются
- Подходит для разработки и тестирования

**Для продакшена рекомендуется:**
- Использовать базу данных (MongoDB, PostgreSQL, SQLite)
- Сохранять настройки напоминаний
- Реализовать отправку уведомлений через cron jobs

## 📝 Как изменить тексты

Все тексты находятся в файле **`config/texts.json`** для удобного редактирования без изменения кода.

### Быстрый старт:

1. **Откройте файл `config/texts.json`** в любом текстовом редакторе
2. **Найдите нужный текст** по ключу (например, `WELCOME`, `SELECT_DOSAGE`)
3. **Измените текст** на нужный вам
4. **Сохраните файл**
5. **Перезапустите бота** для применения изменений

### Форматирование:

- **Переносы строк**: используйте `\n` для новой строки
- **Переменные**: используйте `{time}`, `{timezone}` и т.д. для подстановки значений

### Пример:

```json
{
  "TEXTS": {
    "WELCOME": "Ваш новый текст приветствия\n\nВторая строка",
    "SELECT_DOSAGE": "Вы будете получать уведомления в {time}"
  }
}
```

📖 **Подробная документация**: см. `config/README_TEXTS.md`

После изменения текстов перезапустите бота (если используете `npm start`) или дождитесь автоперезагрузки (если используете `npm run dev`).

## 🔄 User Flow (Пользовательский сценарий)

### Для 1 капсулы:

1. **Старт** (`/start`)
   - Приветственное сообщение
   - Кнопки: "📅 Начать прием витаминов" и "ℹ️ О компании"

2. **Выбор дозировки**
   - Вопрос: "Сколько капсул вы будете принимать за один раз?"
   - Кнопки: "1 капсула ⭐ рекомендовано", "2 капсулы", "Назад"

3. **Выбор часового пояса**
   - Список часовых поясов (UTC+3, UTC+2, и т.д.)
   - Кнопка "Назад"

4. **Выбор времени**
   - Вопрос: "Во сколько вам удобно получать уведомление?"
   - Кнопки: "8:00", "12:00", "18:00", "21:00", "Другое время", "Назад"
   - Если выбрано "Другое время" → ввод времени в формате ЧЧ:ММ

5. **Подтверждение**
   - Сообщение: "Вы будете получать уведомления в [время]"
   - Кнопки: "Подтвердить", "Назад"

6. **Завершение**
   - Сообщение с итоговыми настройками
   - Кнопка: "В главное меню"

### Для 2 капсул:

Шаги 1-3 аналогичны, затем:

4. **Выбор первого времени**
   - Вопрос: "Во сколько вам удобно получать ПЕРВОЕ уведомление?"
   - Те же кнопки выбора времени

5. **Подтверждение первого времени**
   - Подтверждение первого времени

6. **Выбор второго времени**
   - Вопрос: "Во сколько вам удобно получать ВТОРОЕ уведомление?"
   - Те же кнопки выбора времени

7. **Подтверждение второго времени**
   - Подтверждение второго времени

8. **Завершение**
   - Сообщение с итоговыми настройками (оба времени)
   - Кнопка: "В главное меню"

## 🛠️ Технологии

- **Node.js** (ES Modules) - среда выполнения
- **Telegraf** (v4.15.3) - библиотека для работы с Telegram Bot API
- **node-cron** (v3.0.3) - для планирования напоминаний (готово к использованию)
- **dotenv** (v16.3.1) - для работы с переменными окружения

## ❓ Решение проблем

### Бот не запускается

**Ошибка: "BOT_TOKEN не установлен!"**
- Убедитесь, что файл `.env` создан в корне проекта
- Проверьте, что в файле есть строка `BOT_TOKEN=ваш_токен`
- Убедитесь, что токен скопирован полностью (без пробелов)

**Ошибка: "Cannot find module"**
- Выполните `npm install` для установки зависимостей
- Убедитесь, что вы находитесь в правильной директории

**Ошибка при подключении к Telegram**
- Проверьте правильность токена
- Убедитесь, что у вас есть интернет-соединение
- Попробуйте создать нового бота через @BotFather

### Бот не отвечает

- Убедитесь, что бот запущен (видите сообщение "✅ Бот успешно запущен!")
- Проверьте, что вы пишете правильному боту (по username)
- Попробуйте отправить команду `/start` заново
- Перезапустите бота

### Состояния сбрасываются

Это нормально для текущей версии - состояния хранятся в памяти и теряются при перезапуске. Для постоянного хранения нужно подключить базу данных.

## 📦 Расширение функционала

### Добавление новых текстов:
1. Откройте `config/texts.js`
2. Добавьте новую константу в объект `TEXTS`
3. Используйте её в нужном обработчике

### Добавление новых состояний:
1. Добавьте состояние в `config/states.js`:
```javascript
export const USER_STATES = {
  // ... существующие состояния
  NEW_STATE: 'NEW_STATE',
};
```

2. Создайте обработчик в `handlers/newHandler.js`

3. Добавьте роутинг в `index.js`:
```javascript
case USER_STATES.NEW_STATE:
  await newHandler(ctx);
  break;
```

### Подключение базы данных:

Для продакшена рекомендуется:
- Заменить `services/userState.js` на работу с БД (MongoDB, PostgreSQL, SQLite)
- Сохранять настройки напоминаний в БД
- Настроить cron jobs для отправки уведомлений в `services/reminderService.js`

Пример структуры для напоминаний:
```javascript
{
  userId: 123456789,
  capsules: 1,
  time1: "12:00",
  time2: null, // или время для второй капсулы
  timezone: "UTC+3",
  enabled: true
}
```

## 📄 Лицензия

ISC

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте раздел "Решение проблем" выше
2. Убедитесь, что все зависимости установлены
3. Проверьте логи в консоли при запуске бота
