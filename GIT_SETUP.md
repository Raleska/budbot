# 📦 Настройка Git репозитория

Локальный Git репозиторий уже инициализирован и первый коммит создан.

## 🚀 Загрузка на GitHub

### Вариант 1: Через веб-интерфейс GitHub

1. **Создайте новый репозиторий на GitHub:**
   - Перейдите на https://github.com/new
   - Введите название репозитория (например, `telegram-vitamin-bot`)
   - Выберите **Public** или **Private**
   - **НЕ** создавайте README, .gitignore или лицензию (они уже есть)
   - Нажмите "Create repository"

2. **Подключите локальный репозиторий к удаленному:**
   ```bash
   git remote add origin https://github.com/ваш_username/telegram-vitamin-bot.git
   git branch -M main
   git push -u origin main
   ```

### Вариант 2: Через SSH

Если у вас настроен SSH ключ:

```bash
git remote add origin git@github.com:ваш_username/telegram-vitamin-bot.git
git branch -M main
git push -u origin main
```

## 📝 Полезные команды Git

### Проверка статуса
```bash
git status
```

### Просмотр истории коммитов
```bash
git log
```

### Добавление изменений
```bash
git add .
git commit -m "Описание изменений"
git push
```

### Создание новой ветки
```bash
git checkout -b feature/название-функции
```

### Синхронизация с удаленным репозиторием
```bash
git pull origin main
```

## 🔒 Важные замечания

⚠️ **Не коммитьте в Git:**
- Файл `.env` с реальными токенами и паролями
- SSL сертификаты (`.key`, `.pem`, `.crt`)
- `node_modules/` (уже в .gitignore)
- Логи и временные файлы

✅ **Все это уже добавлено в `.gitignore`**

## 📚 Дополнительно

Если нужно изменить имя ветки на `main`:
```bash
git branch -M main
```

Если нужно настроить имя пользователя и email для Git:
```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш@email.com"
```
