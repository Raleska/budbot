# 🚀 Развертывание бота на Ubuntu сервере

Пошаговая инструкция по развертыванию Telegram бота на Ubuntu сервере с доменом от Reg.ru.

## ⚡ Быстрый старт

### Развертывание через Git (рекомендуется)

```bash
# 1. Подключитесь к серверу
ssh root@ваш_сервер

# 2. Клонируйте репозиторий (или используйте скрипт, который сделает это автоматически)
cd /opt
git clone https://github.com/Raleska/budbot.git telegram-bot
cd telegram-bot

# 3. Запустите полный скрипт развертывания
chmod +x deploy/*.sh
sudo ./deploy/deploy-full.sh
```

Скрипт автоматически:
- Установит все зависимости системы (Node.js, PM2, Nginx, Git)
- Клонирует репозиторий из GitHub
- Настроит SSL сертификат
- Установит зависимости Node.js
- Настроит базу данных (если нужно)
- Запустит бота и установит вебхук

### Обновление бота

После внесения изменений в репозиторий:

```bash
# На сервере
sudo ./deploy/deploy-update.sh
```

Или вручную:

```bash
cd /opt/telegram-bot
sudo -u botuser git pull
sudo -u botuser npm install  # если изменились зависимости
sudo -u botuser pm2 restart telegram-bot
```

---

## 📋 Подробная инструкция

## 📋 Предварительные требования

- Ubuntu Server 20.04 или новее
- Домен с настроенными A записями на ваш IP
- SSL сертификат (от Reg.ru или Let's Encrypt)
- Доступ по SSH к серверу с правами root

## 🔧 Шаг 1: Подготовка сервера

### 1.1. Подключение к серверу

```bash
ssh root@ваш_сервер_ip
```

### 1.2. Запуск скрипта развертывания

Запустите скрипт развертывания:

```bash
chmod +x deploy/deploy.sh
sudo ./deploy/deploy.sh
```

Скрипт автоматически:
- Обновит систему
- Установит Node.js 20.x
- Установит PM2 для управления процессами
- Установит Nginx
- Установит Certbot (для Let's Encrypt)
- Настроит базовую конфигурацию Nginx
- Настроит файрвол

## 🔒 Шаг 2: Настройка SSL сертификата

### Вариант A: Использование SSL от Reg.ru

1. **Скачайте файлы сертификата из панели Reg.ru:**
   - Сертификат (Certificate)
   - Private Key (Приватный ключ)
   - Корневой сертификат (Root certificate, если есть)

2. **Загрузите файлы на сервер:**

```bash
# Создайте директорию для сертификатов
sudo mkdir -p /etc/nginx/ssl/hyalpharmbot.ru
sudo chmod 700 /etc/nginx/ssl/hyalpharmbot.ru

# Загрузите файлы (используйте scp или другой способ)
# Пример:
scp certificate.crt root@ваш_сервер:/etc/nginx/ssl/hyalpharmbot.ru/
scp private.key root@ваш_сервер:/etc/nginx/ssl/hyalpharmbot.ru/
scp chain.crt root@ваш_сервер:/etc/nginx/ssl/hyalpharmbot.ru/  # если есть
```

3. **Настройте права доступа:**

```bash
sudo chmod 600 /etc/nginx/ssl/hyalpharmbot.ru/private.key
sudo chmod 644 /etc/nginx/ssl/hyalpharmbot.ru/certificate.crt
```

4. **Запустите скрипт настройки SSL:**

```bash
chmod +x deploy/setup-regru-ssl.sh
sudo ./deploy/setup-regru-ssl.sh
```

### Вариант B: Использование Let's Encrypt (бесплатно)

Если вы выбрали получение бесплатного сертификата в скрипте `deploy.sh`, он будет настроен автоматически.

Или вручную:

```bash
sudo certbot --nginx -d hyalpharmbot.ru
```

## 📦 Шаг 3: Развертывание кода бота

### 3.1. Копирование файлов на сервер

```bash
# На вашем локальном компьютере
scp -r . root@ваш_сервер:/opt/telegram-bot/
```

Или используйте Git:

```bash
# На сервере
cd /opt/telegram-bot
git clone ваш_репозиторий .
```

### 3.2. Установка зависимостей

```bash
cd /opt/telegram-bot
npm install
```

### 3.3. Настройка переменных окружения

Создайте файл `.env` на основе примера:

```bash
cd /opt/telegram-bot
cp deploy/env.production.example .env
nano .env
```

Заполните следующие переменные:

```env
# Telegram Bot Token
BOT_TOKEN=ваш_токен_бота

# Режим работы
USE_DATABASE=true
USE_WEBHOOK=true

# Настройки вебхука
WEBHOOK_URL=https://hyalpharmbot.ru
WEBHOOK_PORT=3000
WEBHOOK_PATH=/webhook
WEBHOOK_SECRET_TOKEN=ваш_секретный_токен_для_безопасности

# Настройки PostgreSQL (если используете БД)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bot_remind
DB_USER=postgres
DB_PASSWORD=ваш_пароль

# Администраторы (опционально)
ADMIN_IDS=ваш_telegram_id
```

**Генерация секретного токена:**

```bash
openssl rand -hex 32
```

### 3.4. Настройка базы данных (если используете)

```bash
# Установка PostgreSQL
sudo apt install postgresql postgresql-contrib

# Создание базы данных
sudo -u postgres psql
CREATE DATABASE bot_remind;
CREATE USER botuser WITH PASSWORD 'ваш_пароль';
GRANT ALL PRIVILEGES ON DATABASE bot_remind TO botuser;
\q

# Инициализация схемы
cd /opt/telegram-bot
node database/init.js
```

## 🚀 Шаг 4: Запуск бота

### 4.1. Запуск с PM2

```bash
cd /opt/telegram-bot
sudo -u botuser pm2 start index.js --name telegram-bot
sudo -u botuser pm2 save
sudo -u botuser pm2 startup
```

### 4.2. Установка вебхука

```bash
cd /opt/telegram-bot
npm run webhook:set
```

Или вручную через скрипт:

```bash
node scripts/setWebhook.js
```

### 4.3. Проверка работы

```bash
# Проверка статуса бота
sudo -u botuser pm2 status

# Просмотр логов
sudo -u botuser pm2 logs telegram-bot

# Проверка вебхука
npm run webhook:info

# Проверка health check
curl https://hyalpharmbot.ru/health
```

## 🔍 Шаг 5: Проверка и мониторинг

### Проверка Nginx

```bash
# Проверка конфигурации
sudo nginx -t

# Просмотр логов
sudo tail -f /var/log/nginx/hyalpharmbot.ru-access.log
sudo tail -f /var/log/nginx/hyalpharmbot.ru-error.log
```

### Проверка работы бота

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Проверьте работу всех функций

## 🔄 Обновление бота

### Автоматическое обновление (рекомендуется)

```bash
# Используйте скрипт обновления
sudo ./deploy/deploy-update.sh
```

Скрипт автоматически:
- Создаст резервную копию
- Остановит бота
- Обновит код через `git pull`
- Установит новые зависимости
- Перезапустит бота
- Покажет статус и логи

### Ручное обновление

```bash
cd /opt/telegram-bot

# Создание резервной копии (рекомендуется)
cp -r /opt/telegram-bot /opt/telegram-bot-backup-$(date +%Y%m%d-%H%M%S)

# Остановка бота
sudo -u botuser pm2 stop telegram-bot

# Обновление кода
sudo -u botuser git fetch origin
sudo -u botuser git reset --hard origin/main

# Установка новых зависимостей
sudo -u botuser npm install

# Запуск бота
sudo -u botuser pm2 restart telegram-bot
sudo -u botuser pm2 save
```

### Откат к предыдущей версии

Если что-то пошло не так:

```bash
cd /opt/telegram-bot
sudo -u botuser git log --oneline  # посмотреть историю коммитов
sudo -u botuser git reset --hard COMMIT_HASH  # вернуться к нужному коммиту
sudo -u botuser pm2 restart telegram-bot
```

## 🛠️ Полезные команды

### PM2

```bash
# Статус
sudo -u botuser pm2 status

# Логи
sudo -u botuser pm2 logs telegram-bot

# Перезапуск
sudo -u botuser pm2 restart telegram-bot

# Остановка
sudo -u botuser pm2 stop telegram-bot

# Удаление из автозапуска
sudo -u botuser pm2 delete telegram-bot
```

### Nginx

```bash
# Перезагрузка конфигурации
sudo systemctl reload nginx

# Перезапуск
sudo systemctl restart nginx

# Статус
sudo systemctl status nginx
```

### Вебхук

```bash
# Информация о вебхуке
npm run webhook:info

# Удаление вебхука
npm run webhook:delete

# Установка вебхука
npm run webhook:set
```

## 🔒 Безопасность

1. **Firewall:** Убедитесь, что открыты только необходимые порты:
   ```bash
   sudo ufw status
   ```

2. **SSL:** Проверьте, что SSL сертификат действителен:
   ```bash
   openssl s_client -connect hyalpharmbot.ru:443 -servername hyalpharmbot.ru
   ```

3. **Secret Token:** Обязательно используйте `WEBHOOK_SECRET_TOKEN` для защиты вебхука

4. **Права доступа:** Убедитесь, что файлы имеют правильные права:
   ```bash
   sudo chown -R botuser:botuser /opt/telegram-bot
   sudo chmod 600 /opt/telegram-bot/.env
   ```

## ❓ Решение проблем

### Бот не отвечает

1. Проверьте логи PM2:
   ```bash
   sudo -u botuser pm2 logs telegram-bot
   ```

2. Проверьте статус вебхука:
   ```bash
   npm run webhook:info
   ```

3. Проверьте доступность эндпоинта:
   ```bash
   curl https://hyalpharmbot.ru/webhook
   ```

### Ошибки SSL

1. Проверьте конфигурацию Nginx:
   ```bash
   sudo nginx -t
   ```

2. Проверьте права доступа к файлам сертификата:
   ```bash
   ls -la /etc/nginx/ssl/hyalpharmbot.ru/
   ```

### Ошибки подключения к БД

1. Проверьте, что PostgreSQL запущен:
   ```bash
   sudo systemctl status postgresql
   ```

2. Проверьте подключение:
   ```bash
   psql -U postgres -d bot_remind
   ```

## 📚 Дополнительная документация

- `README_WEBHOOK.md` - Подробная информация о вебхуках
- `README_DATABASE.md` - Настройка PostgreSQL
- `EDIT_TEXTS.md` - Редактирование текстов бота
