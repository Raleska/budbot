# 📝 Редактирование текстов бота

Все тексты бота хранятся в файле **`config/texts.json`** для удобного редактирования.

## 🚀 Быстрый старт

1. Откройте файл `config/texts.json`
2. Найдите нужный текст по ключу
3. Измените текст
4. Сохраните файл
5. Перезапустите бота

## 📋 Основные тексты

- `WELCOME` - Приветствие при `/start`
- `SELECT_DOSAGE` - Выбор частоты приема
- `SELECT_TIME_SINGLE` - Выбор времени (один раз в день)
- `SELECT_TIME_FIRST` - Выбор первого времени (два раза в день)
- `SELECT_TIME_SECOND` - Выбор второго времени (два раза в день)
- `REMINDER_SET_SINGLE` - Сообщение после создания (один раз в день)
- `REMINDER_SET_DOUBLE` - Сообщение после создания (два раза в день)
- `REMINDER_MESSAGE` - Текст ежедневного напоминания
- `ABOUT_COMPANY` - Текст раздела "О компании"

## 🔤 Переменные в текстах

Используйте фигурные скобки для подстановки значений:
- `{time}` - время напоминания
- `{time1}` - первое время
- `{time2}` - второе время
- `{timezone}` - часовой пояс

Пример:
```json
"CONFIRM_TIME_SINGLE": "Вы будете получать уведомления в {time}"
```

## 📄 Подробная документация

См. `config/README_TEXTS.md` для полного списка всех текстов и инструкций.
